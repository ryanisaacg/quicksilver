language: rust
os:
  - linux
  - osx
addons:
  apt:
    packages:
      - libasound2-dev
      - libudev-dev
rust:
  - stable
  - beta
  - nightly
matrix:
  allow_failures:
    - rust: nightly
  fast_finish: true
